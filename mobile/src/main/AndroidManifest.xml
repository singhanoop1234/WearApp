<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tcs.wearapp" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.UserActionActivity"/>
        <activity android:name=".activity.MyActivity" />
        <activity android:name=".activity.NotificationLandingActivity" />
        <activity android:name=".activity.NotificationActivity1" />

        <service android:name=".service.MonitorUserTrendsService" />

        <activity
            android:name=".activity.TnCActivity"
            android:label="@string/title_activity_tn_c" >
        </activity>
    </application>

</manifest>
